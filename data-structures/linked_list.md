# Linked List

A linked list is a linear data structure where each element (node) points to the next element.
It consists of nodes, where each node contains data and a reference (or link) to the next node.